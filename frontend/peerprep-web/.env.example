# Frontend Environment Variables
# Copy this file to .env.development (for local) or .env.production (for production)

# === REQUIRED ===

# Backend API base URL
# Local development: http://localhost:4001
# Production: https://your-backend-domain (or CloudFront URL)
VITE_API_BASE=http://localhost:4001

# Firebase Configuration
# Get these from Firebase Console > Project Settings > Your apps > Firebase SDK snippet
VITE_FB_API_KEY=your-firebase-api-key
VITE_FB_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FB_PROJECT_ID=your-project-id
VITE_FB_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FB_MESSAGING_SENDER_ID=your-sender-id
VITE_FB_APP_ID=your-app-id
VITE_FB_MEASUREMENT_ID=G-your-measurement-id

# === NOTES ===
# - All Vite env vars must start with VITE_ to be exposed to the frontend
# - These values are PUBLIC and will be visible in the browser
# - Firebase config values are safe to expose (they're not secrets)
# - For production, use your actual backend URL (not localhost)
