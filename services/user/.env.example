# User Service Environment Variables
# Copy this file to .env and fill in the actual values

# === REQUIRED ===

# MongoDB connection string for user data
MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/peerprep?retryWrites=true&w=majority

# CORS origin - URL of your frontend
# Local development: http://localhost:5173
# Production: https://your-cloudfront-domain.cloudfront.net
CORS_ORIGIN=http://localhost:5173

# Firebase Admin SDK service account credentials
# Get this from Firebase Console > Project Settings > Service Accounts > Generate new private key
# IMPORTANT: This is required for Firebase authentication to work
# For local development, you can either:
#   1. Set GOOGLE_APPLICATION_CREDENTIALS to path of JSON file, OR
#   2. Set this inline (see local .env for example)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccount.json

# === OPTIONAL ===

# Port number (defaults to 4001 if not set)
PORT=4001

# Node environment (set automatically by docker-compose)
# NODE_ENV=production
